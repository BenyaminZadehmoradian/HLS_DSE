# Evidence Store

`measured/` contains immutable raw/normalized measurements. `derived/` contains deterministic transformations. `verified/` contains human-reviewed evidence.

CSV is an export/view format, not the source of truth. JSON/JSONL evidence records and immutable raw artifacts remain authoritative.
