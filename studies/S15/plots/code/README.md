# Plot code

One file per plot.

Naming:
`plot_<plot_id>_<short_name>.py`

A plot script should:
1. load declared inputs;
2. validate required columns;
3. generate exactly its declared figure(s);
4. save to `../generated/`;
5. emit/update its provenance manifest.
