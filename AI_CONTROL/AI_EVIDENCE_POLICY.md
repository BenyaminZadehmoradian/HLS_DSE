# AI EVIDENCE POLICY
Evidence lifecycle:
Tool output → raw artifact → parser → normalized evidence → derived metric → report

Every evidence item identifies study_id, run_id, evaluation_id, source artifact, metric, value,
unit, status/type, parser/version, environment fingerprint, and input identity.
