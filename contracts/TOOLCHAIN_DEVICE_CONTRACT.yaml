schema_version: '1.0'
required:
  vendor: required
  device_part: required
  board: optional
  package: optional
  speed_grade: optional
  vivado_version: required_when_used
  vitis_hls_version: required_when_used
  os_image: required
  target_clock: required
  clock_uncertainty: required_when_used
  synthesis_strategy: required_when_used
  implementation_strategy: required_when_used
  seeds: required_when_stochastic
identity_rule: Toolchain/device differences create distinct environment IDs.
device_part: xc7z020clg484
vendor: AMD-Xilinx
family: Zynq-7000
package: clg484
speed_grade: UNKNOWN_UNTIL_FIXED
