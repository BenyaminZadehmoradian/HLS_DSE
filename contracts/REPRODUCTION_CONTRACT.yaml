contract_version: "1.0"
contract_id: REPRODUCTION_CONTRACT
status: ACTIVE
required_identity:
  - project_version
  - git_commit
  - study_id
  - study_version
  - run_id
  - device_part
  - tool_versions
  - benchmark_commit
  - dataset_version
  - configuration_hash
  - environment_hash
  - seed
rules:
  - "A result is reproducible only if its provenance chain can be resolved."
  - "Cross-paper numerical comparison requires normalization or is prohibited."
  - "External baseline claims require a recorded source version/commit."
