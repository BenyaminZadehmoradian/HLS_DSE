project_version: V23.2
reference_device: xc7z020clg484
required_tools:
  vivado: required
  vitis: required
  vitis_run: required          # 2025.2.1 ships no standalone vitis_hls; the gated project shim maps it to vitis-run --mode hls
  python: required
hardware_execution: optional_until_P1_hardware_smoke
checks:
  - python_version
  - git
  - vivado_version
  - vitis_version
  - vitis_run_version
  - device_support
  - license_visibility
  - disk_space
  - cpu_memory
  - project_contracts
